//Learned comparison operators return a boolean value and learned how to use the equality operator (==) which differs from assignment (=)

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
