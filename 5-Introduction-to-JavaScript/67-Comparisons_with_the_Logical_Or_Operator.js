//Learned how to use the logical or operator (||) to use either operand to complete a true/false statements

function testLogicalOr(val) {
  // Only change code below this line

  if (val  > 20 || val < 10) {
    return "Outside";
  }

  // Only change code above this line
  return "Inside";
}

// Change this value to test
testLogicalOr(15);
