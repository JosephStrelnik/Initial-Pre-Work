//Learned what the inequality operator (!=) is and how it relates in usage and definition versus the equality operator

// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
