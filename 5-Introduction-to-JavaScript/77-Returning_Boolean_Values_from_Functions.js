//Learned how to return boolean values using if/else statements or more effectively, using === to return true/False

function isLess(a, b) {
  // Fix this code
  return a <= b;

}

// Change these values to test
isLess(10, 15);
